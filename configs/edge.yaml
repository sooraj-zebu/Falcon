app:
  name: Falcon Edge
  version: 1.0.0

edge:
  name: falcon-edge-tvm

core:
  host: 127.0.0.1
  grpc_port: 12167

logging:
  level: info

storage:
  data_dir: data
  log_dir: logs
  mount_path: /root/Falcon/dev-storage
